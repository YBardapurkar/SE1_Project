<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.se1.team3.campuscarrental">

    <meta-data
        android:name="AA_DB_NAME"
        android:value="campusCarRental.db" />
    <meta-data
        android:name="AA_DB_VERSION"
        android:value="1" />

    <application
        android:name=".CampusCarRentalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".AdminEditsProfile" />
        <activity android:name=".AdminViewsUserDetailsActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".AdminHomeActivity" />
        <activity android:name=".SearchUserActivity" android:label="@string/app_name" />
        <activity android:name=".RentalManagerHomeActivity" />
        <activity android:name=".UserHomeActivity" />
        <activity android:name=".UserProfileActivity" />
        <activity android:name=".UserUpdateProfileActivity" />
        <activity android:name=".SignUpActivity" />
        <activity
            android:name=".LoginActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:screenOrientation="portrait" />
        <activity
            android:name=".SplashActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter><action android:name="android.intent.action.SEARCH" /></intent-filter>

        </activity>
    </application>

</manifest>